import { initialState } from './index'

const CartReducer = (state = initialState.Snackbar, action) => {
    switch(action.type){
      default:
        return state
    }
}

export default CartReducer